apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  # This creates a public-facing AWS Network Load Balancer to expose our service.
  type: LoadBalancer
